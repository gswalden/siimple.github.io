<!-- Panels -->
<h3>PANELS</h3>

<!-- Default panels -->
<p>A panel consists in one <code>div</code> with the class <code>.panel</code>, and two <code>div</code> inside,
the first with the class <code>.panel-head</code> and the second with the class <code>.panel-body</code>.</p>

<!-- Default panels demo -->
<div class="panel">
  <div class="panel-head">Panel</div>
  <div class="panel-body">This is the content of the panel</div>
</div>

<pre>
&lt;div class="panel"&gt;
  &lt;div class="panel-head"&gt;Panel&lt;/div&gt;
  &lt;div class="panel-body"&gt;Content&lt;/div&gt;
&lt;/div&gt;
</pre>
